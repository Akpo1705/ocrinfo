import React from 'react';
import UserProfile from '../../feature/userProfile/UserProfile';

const User: React.FC = () => {
  return (
    <div>
      <UserProfile />
    </div>
  );
};

export default User;
